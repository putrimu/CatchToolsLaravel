language: php

php:
  - 7.2.4
  - 5.6
  - 7.0

before_script:
  - Composer install --dev --prefer-source --no-interaction
  - php artisan key:generate
  - cp .env.travis .env

script:
  - vendor/bin/phpunit